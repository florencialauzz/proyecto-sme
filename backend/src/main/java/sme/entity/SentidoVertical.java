package sme.entity;

// Espejo del ENUM sentido_vertical de contratos/esquema-bd.md — solo Rampa
// (RF-18). Es una pregunta distinta de la direccion: la direccion dice hacia
// dónde se mueve el vehículo en el plano, esto dice si sube o baja de piso.
public enum SentidoVertical {
    SUBE, BAJA
}

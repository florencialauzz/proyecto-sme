namespace Sme.Grid
{
    // Espejo del ENUM sentido_vertical de contratos/esquema-bd.md — solo
    // Rampa (RF-18). Distinto de la orientación horizontal (CaraAcceso): la
    // flecha dice hacia dónde sale el vehículo en el plano, esto dice si sube
    // o baja de piso.
    public enum SentidoVertical
    {
        SUBE,
        BAJA
    }
}
